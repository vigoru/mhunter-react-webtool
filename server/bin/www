#!/usr/bin/env node

const app = require('../index');

// === Run Server ===
const port = process.env.PORT || 3001;
app.listen(port, () => {
    console.log(`http://localhost:${port}/`);
});
